# 🐧 Інструкція для Git Bash / WSL

## ⚡ Для роботи в Git Bash використовуйте .sh файли!

---

## 📋 Команди для Bash:

### Збірка EXE файлу:

```bash
./build_exe.sh
```

### Створення установщика:

```bash
./build_installer.sh
```

### Тестування EXE:

```bash
./test_exe.sh
```

### Повна збірка (все разом):

```bash
./build_all.sh
```

---

## ⚠️ Важливо!

### У Git Bash НЕ працює:

```bash
❌ build_exe.bat        # Не працює!
❌ test_exe.bat         # Не працює!
```

### Правильно в Git Bash:

```bash
✅ ./build_exe.sh       # Працює!
✅ ./test_exe.sh        # Працює!
```

### АБО через cmd:

```bash
✅ cmd //c build_exe.bat
✅ cmd //c test_exe.bat
```

---

## 📁 Структура скриптів:

| Bash (Git Bash)        | Windows (CMD)         | Призначення |
| ---------------------- | --------------------- | ----------- |
| `./build_exe.sh`       | `build_exe.bat`       | Збірка EXE  |
| `./build_installer.sh` | `build_installer.bat` | Установщик  |
| `./build_all.sh`       | `build_all.bat`       | Все разом   |
| `./test_exe.sh`        | `test_exe.bat`        | Тестування  |

---

## 🚀 Швидкий старт в Bash:

```bash
# 1. Активувати віртуальне середовище
source .venv/Scripts/activate

# 2. Зібрати EXE
./build_exe.sh

# 3. Протестувати (опціонально)
./test_exe.sh

# 4. Створити установщик (потребує Inno Setup)
./build_installer.sh
```

---

## 🔧 Встановлення Inno Setup:

1. Завантажити: https://jrsoftware.org/isdl.php
2. Встановити (стандартне встановлення)
3. **Додати до PATH** (важливо для bash!):
   - Відкрити "Змінні середовища"
   - Додати: `C:\Program Files (x86)\Inno Setup 6\`
   - Перезапустити bash

Після цього команда `iscc` буде доступна в bash.

---

## 💡 Підказки:

### Якщо скрипт не запускається:

```bash
# Надати права на виконання
chmod +x *.sh

# Запустити
./build_exe.sh
```

### Якщо потрібен Windows CMD:

```bash
# Відкрити CMD з поточної папки
cmd //c start cmd

# Або запустити .bat через cmd
cmd //c build_exe.bat
```

### Перевірка створених файлів:

```bash
# Список файлів в dist
ls -lh dist/

# Розмір EXE
du -h dist/RadioSvitlo.exe

# Список установщиків
ls -lh installer_output/
```

---

## ✅ Ваш поточний статус:

- ✓ EXE файл створено: `dist/RadioSvitlo.exe`
- ✓ Bash скрипти готові
- ⏳ Потрібно встановити Inno Setup для створення установщика

---

## 🎯 Наступний крок:

```bash
# Якщо Inno Setup ще не встановлено:
# 1. Відкрити: https://jrsoftware.org/isdl.php
# 2. Завантажити та встановити
# 3. Додати до PATH

# Після встановлення Inno Setup:
./build_installer.sh

# АБО через Windows CMD:
cmd //c build_installer.bat
```

---

**Зараз ви можете запустити:** `./test_exe.sh` **для тестування EXE!** 🚀
