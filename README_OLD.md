# Радіо Світло - Десктопний додаток

Десктопний додаток для Windows для прослуховування онлайн-радіо "Радіо Світло".

## 📋 Вимоги

- Python 3.8 або новіше
- Windows 10/11 (рекомендовано)
- Інтернет-з'єднання

## 🚀 Встановлення залежностей

1. Відкрийте командний рядок або PowerShell у папці проєкту
2. Встановіть необхідні бібліотеки:

```bash
pip install -r requirements.txt
```

Для Windows 10/11 також потрібен Edge WebView2 Runtime (зазвичай вже встановлений):

- Якщо потрібно, завантажте з: https://developer.microsoft.com/microsoft-edge/webview2/

## ▶️ Запуск додатка (режим розробки)

```bash
python radio_svitlo.py
```

## 📦 Компіляція у .exe файл

### Крок 1: Встановлення PyInstaller

```bash
pip install pyinstaller
```

### Крок 2: Базова компіляція

```bash
pyinstaller --onefile --windowed --name="RadioSvitlo" radio_svitlo.py
```

### Крок 3: Компіляція з іконкою (якщо є файл icon.ico)

```bash
pyinstaller --onefile --windowed --name="RadioSvitlo" --icon=icon.ico radio_svitlo.py
```

### Крок 4: Розширена компіляція з оптимізацією

Створіть файл `build.bat` з таким вмістом:

```batch
@echo off
pyinstaller ^
  --onefile ^
  --windowed ^
  --name="RadioSvitlo" ^
  --clean ^
  --noconfirm ^
  radio_svitlo.py

echo.
echo ✅ Компіляція завершена!
echo 📁 Файл знаходиться в папці: dist\RadioSvitlo.exe
pause
```

Потім просто запустіть `build.bat`

### Параметри PyInstaller:

- `--onefile` - створює один виконуваний файл
- `--windowed` - приховує консоль (для GUI додатків)
- `--name` - назва вихідного файлу
- `--icon` - додає іконку до .exe файлу
- `--clean` - очищує кеш перед збіркою
- `--noconfirm` - не питає підтвердження при перезаписі

### Крок 5: Знайти готовий .exe файл

Після компіляції файл буде у папці:

```
dist\RadioSvitlo.exe
```

## 🎯 Функціонал додатка

✅ **Перевірка інтернету** - при запуску перевіряється наявність з'єднання  
✅ **Підтвердження виходу** - запит перед закриттям додатка  
✅ **Центрування вікна** - автоматичне розміщення по центру екрана  
✅ **Розмір вікна** - 1000x700 пікселів  
✅ **Мінімальний розмір** - 800x600 пікселів  
✅ **Можливість зміни розміру** - так  
✅ **Нативний вигляд** - без меню браузера

## ⌨️ Гарячі клавіші

- `F11` - повноекранний режим (стандартна функція браузера)
- `Alt+F4` - закрити додаток

## 🛠️ Налаштування

Ви можете змінити параметри у файлі `radio_svitlo.py`:

```python
self.url = "https://www.radio-svitlo.com"  # URL радіо
self.title = "Радіо Світло"                # Назва вікна
self.width = 1000                           # Ширина вікна
self.height = 700                           # Висота вікна
```

## 📝 Додаткові налаштування для PyInstaller

### Додавання іконки

1. Створіть або завантажте файл іконки `icon.ico` (розмір 256x256)
2. Помістіть його у папку проєкту
3. Використовуйте параметр `--icon=icon.ico` при компіляції

### Додавання метаданих (версія, опис тощо)

Створіть файл `version_info.txt`:

```
VSVersionInfo(
  ffi=FixedFileInfo(
    filevers=(1, 0, 0, 0),
    prodvers=(1, 0, 0, 0),
    mask=0x3f,
    flags=0x0,
    OS=0x40004,
    fileType=0x1,
    subtype=0x0,
    date=(0, 0)
  ),
  kids=[
    StringFileInfo(
      [
      StringTable(
        u'040904B0',
        [StringStruct(u'CompanyName', u'Your Company'),
        StringStruct(u'FileDescription', u'Радіо Світло Desktop App'),
        StringStruct(u'FileVersion', u'1.0.0.0'),
        StringStruct(u'InternalName', u'RadioSvitlo'),
        StringStruct(u'LegalCopyright', u'Copyright © 2026'),
        StringStruct(u'OriginalFilename', u'RadioSvitlo.exe'),
        StringStruct(u'ProductName', u'Радіо Світло'),
        StringStruct(u'ProductVersion', u'1.0.0.0')])
      ])
  ]
)
```

Потім додайте параметр: `--version-file=version_info.txt`

## 🐛 Усунення проблем

### Помилка: "Edge WebView2 not found"

- Встановіть Edge WebView2 Runtime з офіційного сайту Microsoft

### Додаток не запускається після компіляції

- Перевірте, чи всі залежності включені
- Спробуйте компіляцію без `--onefile` (буде папка з бібліотеками)

### Велікий розмір .exe файлу

- Це нормально для PyInstaller (зазвичай 10-30 МБ)
- Можна використати UPX для стиснення: `--upx-dir=path/to/upx`

## 📧 Контакти

Радіо Світло: https://www.radio-svitlo.com

---

**Насолоджуйтесь прослуховуванням! 🎵**
