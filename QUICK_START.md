# 🎯 КОРОТКИЙ ГАЙД: Як створити установщик

## ✅ Що вже зроблено:

1. ✓ EXE файл успішно створено: `dist\RadioSvitlo.exe`
2. ✓ Всі скрипти та конфігурації готові
3. ✓ Іконка logo_svitlo інтегрована всюди

---

## 📋 Що потрібно зробити далі:

### Крок 1️⃣: Встановити Inno Setup

1. Перейдіть на: **https://jrsoftware.org/isdl.php**
2. Завантажте **Inno Setup 6** (останню версію)
3. Встановіть на комп'ютер (стандартне встановлення)
4. Готово! ✓

### Крок 2️⃣: Створити установщик

Після встановлення Inno Setup просто запустіть:

**В Git Bash:**

```bash
./build_installer.sh
```

**В Windows CMD:**

```cmd
build_installer.bat
```

**АБО повна збірка (EXE + установщик):**

Git Bash: `./build_all.sh` | Windows CMD: `build_all.bat`

---

## 📦 Результат:

Після виконання ви отримаєте:

```
installer_output/
└── RadioSvitlo_Setup_v1.0.0.exe  ← ЦЕ ВАШ УСТАНОВЩИК!
```

Розмір: ~30-40 МБ (стиснутий)

---

## 🚀 Використання установщика:

**Для ваших користувачів:**

1. Завантажити `RadioSvitlo_Setup_v1.0.0.exe`
2. Запустити подвійним кліком
3. Натиснути "Далі" 2-3 рази
4. Готово! Програма встановлена! ✓

**Що отримує користувач:**

- ✅ Іконка на робочому столі
- ✅ Програма в меню Пуск
- ✅ Можливість видалити через Панель керування
- ✅ Всюди ваша іконка logo_svitlo

---

## 🎨 Іконки всюди:

Ваш логотип **logo_svitlo** використовується:

- ✅ У вікні програми (під час роботи)
- ✅ В EXE файлі (іконка в провіднику)
- ✅ В установщику (іконка інсталятора)
- ✅ На робочому столі (ярлик)
- ✅ В меню Пуск (ярлик)
- ✅ У списку програм Windows

---

## 📋 Доступні скрипти:

| Скрипт                | Призначення                     |
| --------------------- | ------------------------------- |
| `build_exe.bat`       | Створити тільки EXE файл        |
| `build_installer.bat` | Створити тільки установщик      |
| `build_all.bat`       | Створити все (EXE + установщик) |
| `test_exe.bat`        | Протестувати EXE файл           |

---

## 🔄 Якщо потрібно оновити додаток:

1. Внесіть зміни в `radio_svitlo.py`
2. Змініть версію в `installer.iss` (рядок 6):
   ```pascal
   #define MyAppVersion "1.1.0"
   ```
3. Запустіть `build_all.bat`
4. Готово! Новий установщик створено!

---

## ⚡ Швидкий старт ПРЯМО ЗАРАЗ:

**Для Git Bash:**

```bash
# Якщо Inno Setup ще не встановлено:
# 1. Відкрити https://jrsoftware.org/isdl.php
# 2. Завантажити та встановити

# Після встановлення Inno Setup:
./build_installer.sh

# АБО одразу все:
./build_all.sh
```

**Для Windows CMD:**

```cmd
# Після встановлення Inno Setup:
build_installer.bat

# АБО одразу все:
build_all.bat
```

📖 **Детальна інструкція для Git Bash:** [BASH_GUIDE.md](BASH_GUIDE.md)

---

## ❓ Питання та відповіді:

**Q: Чи потрібен Python користувачам?**  
A: НІ! EXE та установщик самодостатні.

**Q: Чи працює на всіх Windows?**  
A: Так! Windows 10/11 точно. Windows 7/8 - можливо.

**Q: Скільки важить установщик?**  
A: ~30-40 МБ (стиснутий LZMA2)

**Q: Чи безпечний EXE файл?**  
A: Так! Windows може показати попередження через відсутність цифрового підпису.

**Q: Як підписати код?**  
A: Потрібен сертифікат Code Signing (~$100-400/рік)

---

## 🎉 ВСЕ ГОТОВО!

Ваш проект повністю налаштований для створення професійного установщика!

Просто встановіть **Inno Setup** та запустіть `build_installer.bat`! 🚀
